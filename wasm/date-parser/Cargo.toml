[package]
name = "date-parser"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
chrono = "0.4"
wasm-bindgen = "0.2"
time = { version = "0.3", features = ["parsing", "formatting", "macros"] }
js-sys = "0.3"
web-sys = { version = "0.3", features = ["console"] }